#include <stdio.h>

int main(){
    int K;
    scanf("%d", &K);

    for (int i = 0; i < K; i++){
        int N;
        scanf("%d", &N);

        int MMR[N];
        for (int j = 0; j < N; j++){
            scanf("%d", &MMR[j]);
        }

        int F;
        scanf("%d", &F);
    }
    
    return 0;
}